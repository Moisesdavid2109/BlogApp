# ─── PostgreSQL (database) ───
POSTGRES_USER=blog
POSTGRES_PASSWORD=blog_password
POSTGRES_DB=blog

# ─── Backend (API) ───
NODE_ENV=production
PORT=3000
DB_HOST=database
DB_PORT=5432
DB_USER=blog
DB_PASSWORD=blog_password
DB_NAME=blog

# ─── Frontend (Vite, solo se usa en build) ───
VITE_API_URL=http://localhost:3000/api
