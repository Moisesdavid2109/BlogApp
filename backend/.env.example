# Backend — variables de entorno para desarrollo local (sin Docker)
# Copia este archivo como .env y ajusta los valores si es necesario.

NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=blog
DB_PASSWORD=blog_password
DB_NAME=blog
